name: ailog_flutter_example
description: Minimal runnable example for ailog_flutter.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0
  flutter: ">=3.22.0"

dependencies:
  ailog_flutter:
    path: ../
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0

flutter:
