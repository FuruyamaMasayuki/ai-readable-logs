name: ailog_vault_example
description: Minimal runnable example for ailog_vault.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0
  flutter: ">=3.22.0"

dependencies:
  ailog:
    path: ../../ailog
  ailog_flutter:
    path: ../../ailog_flutter
  ailog_vault:
    path: ../
  flutter:
    sdk: flutter
  path_provider: ^2.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^4.0.0
