# ailog_vault example

A minimal app with three buttons: log an event, log an error, and **Share
logs** — which zips `app.jsonl` (+ rotations) together with a freshly
generated `digest.md` and opens the platform share sheet.

Run on a device or simulator:

```sh
flutter run
```
